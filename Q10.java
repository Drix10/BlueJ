public class Q10
{
    public static void main()
    {
      double a=-7;
      double b=-2;
      double c=-9;
      double d=32.17;
      double e=-6.7;
      double x=Math.max(a,Math.min(b,c));
      double y=Math.ceil(d);
      double z=Math.abs(e);
      System.out.println("the greatest num is"+x+"the roundof is"+y+"the absolute value is"+z);
    }
}